package Chess;

import org.testng.Assert;

import static Chess.Player.BLACK;
import static org.testng.TestNGAntTask.Mode.junit;

public class Test {

    //start testing here
    //I still don't understand the move method at all.

    Player black = BLACK;
    Player white = Player.WHITE;

    King test = new King(BLACK);
    Move umm = new Move(2,2,3,3);


}
